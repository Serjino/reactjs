import * as React from 'react';
import styles from '../shared/header.scss';

export function Header () {
    return (
        <header className={styles.header}>
            Hello React!!! АРКАША
        </header>
    );
}